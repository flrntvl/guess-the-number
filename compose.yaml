services:
  guess:
    build: .
    stdin_open: true
    tty: true
    volumes:
      - ./data:/app/data
      - ./lib:/app/lib
      - ./spec:/app/spec
      - ./bin:/app/bin
      - ./coverage:/app/coverage